Once all the packages are installed run the file using- python3 neuralnet.py

Make sure the csv file destination is correct. Currently it is assumed to be in the same folder as the python file.
(csv is also submitted along with the code)

The code to perfrom grid search is also commented out.
To perfrom grid search uncomment the following lines: 42, 68, 94, 120